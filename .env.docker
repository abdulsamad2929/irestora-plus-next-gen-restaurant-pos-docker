# Database Configuration
DB_HOST=db
DB_USER=irestora
DB_PASSWORD=SecurePassword123!
DB_NAME=irestora_db
DB_PORT=3306

# MySQL Root Password (use a strong password in production)
MYSQL_ROOT_PASSWORD=RootPassword123!

# Redis Configuration
REDIS_PASSWORD=RedisPassword123!

# Web Server Configuration
WEB_PORT=80
WEB_HTTPS_PORT=443

# PHP Configuration
PHP_DISPLAY_ERRORS=0
PHP_ERROR_REPORTING=E_ALL
PHP_MEMORY_LIMIT=512M
PHP_UPLOAD_MAX_FILESIZE=256M
PHP_POST_MAX_SIZE=256M
PHP_MAX_EXECUTION_TIME=300

# Application Environment (production | development; use ./deploy.sh --dev for development)
APP_ENV=production
APP_DEBUG=false
APP_KEY=

# Timezone
TZ=UTC
